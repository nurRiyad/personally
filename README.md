# Personally

Phase 0 foundation. See [`docs/PROJECT_FOUNDATION.md`](./docs/PROJECT_FOUNDATION.md) for scope and architecture. Use Node 24 (`nvm use`), then `pnpm install`, `pnpm setup:local`, and `pnpm dev`.

Web: http://localhost:3000 · API: http://localhost:8787
